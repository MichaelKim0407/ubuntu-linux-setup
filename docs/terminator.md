# Terminator

Configures Terminator — enables infinite scrollback and suppresses the multiple terminal close dialog.

Run as the non-root user from the repo root:

```bash
python3 install/terminator.py
```
